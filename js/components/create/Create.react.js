/** @jsx React.DOM */

var Form = require('./CreateForm.react');
var FormHeader = require('./CreateFormHeader.react');

var Create = {
  Form: Form,
  FormHeader: FormHeader
};

module.exports = Create;
