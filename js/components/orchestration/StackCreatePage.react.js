/** @jsx React.DOM */

var React = require('react');

var StackCreatePage = React.createClass({
  render: function () {
    return (
      <ViewContainer>Hello</ViewContainer>
    );
  }
});

module.exports = StackCreatePage;
